global:
  scrape_interval: 15s

rule_files:
  - "/etc/prometheus/alert.rules.yml"

scrape_configs:
  - job_name: "user-crud-prod"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["app-prod:5500"]